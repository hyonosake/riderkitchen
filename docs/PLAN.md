# Rider Kitchen — план работ

Цель: сделать локальную версию сайта кейтеринг-сервиса, поправить баги,
аккуратно разбить на компоненты, добавить новые фичи — и презентовать
результат владельцу.

Стек: **React + TypeScript + Vite**. Линтер — oxlint (уже настроен,
быстрый, без лишнего конфига). Без state-менеджеров, роутеров и т.п.,
пока не понадобится — сайт в основном контентный.

Статус: этапы 1–4 выполнены (перенос, баги, компоненты, фичи — плюс
тестовая инфраструктура и рефакторинг R0–R6); впереди этап 5 —
презентация владельцу. Сайт оригинала — WordPress + Elementor
(riderkitchen.ru), доступа к исходникам/хостингу нет, поэтому контент
выгружен разбором публичного HTML, а не 1:1 копией файлов.

## Как запускать

```bash
npm install      # один раз
npm run dev      # локальный сервер с hot-reload
npm run build    # прод-сборка в dist/ (проверяет типы + собирает)
npm run preview  # локально посмотреть прод-сборку
npm run lint      # быстрый линт (oxlint)
```

## Этап 0 — подготовка (сделано)

- [x] Vite + React + TS scaffold (`npm create vite`)
- [x] Базовая структура папок (`src/components`, `src/pages`, `src/content`)
- [x] Убран демо-контент шаблона
- [x] URL сайта: https://riderkitchen.ru/
- [ ] Прислать список задуманных фич (можно позже, перед этапом 4)

## Этап 1 — перенос сайта на локаль (в основном сделано)

Доступа к исходникам/хостингу нет, перенесли "по образу" с публичной
версии — разобрали HTML напрямую (сайт на WordPress/Elementor), а не
скопировали вёрстку 1:1 (она там в виде тяжёлого Elementor-супа из
div'ов, копировать её как основу для React не имеет смысла).

Сделано:

- [x] Меню полностью перенесено и типизировано: `src/content/menu.ts`,
      `src/content/types.ts` (7 категорий блюд + готовые сеты, ~60
      позиций с описаниями, ценами, тегами vegan/сезонное/от 5 порций).
- [x] Фото — для всех блюд и сетов, кроме гарниров и напитков (там их
      нет и на оригинале). Сопоставлены с блюдами по порядку в
      разметке (фото идёт прямо перед названием), а не только по имени
      файла — часть файлов названа технически (MagicEraser/Bazaart/IMG),
      без привязки к блюду в самом имени. Все скачаны в `src/assets/menu/`.
- [x] Контакты (телефон, WhatsApp, Instagram) — `src/content/menu.ts`.
- [x] Базовая структура страницы: Header (навигация по разделам),
      Hero, секции меню, готовые сеты, Footer.
- [x] Собран рабочий React-рендер, проверен в браузере (desktop,
      светлая/тёмная тема) — `npm run dev`.

Осталось до полного закрытия этапа:

- [ ] Визуально сверить с оригиналом на мобильной ширине (сейчас
      раскладка на общих адаптивных принципах CSS grid, а не пиксельная
      копия — см. решение ниже). Первый проход сделан (см. находки
      ниже), нужна сверка непосредственно с оригиналом.
- [x] Оптимизировать вес фото — все PNG (`src/assets/menu/` + `hero.png`)
      сконвертированы в WebP через `cwebp` (q82, ресайз короткой
      стороны до 900px там, где было больше — под retina-карточки
      ~260-450px). `src/assets/` уменьшился с ~85 МБ до ~6 МБ.
- [x] Новая цветовая палитра и типографика: `#262626` (тёмный) /
      `#FFE8DB` (светлый персиковый) как основные цвета светлой/тёмной
      темы (тёмная — по сути инверсия светлой); шрифты — Fuzzy Bubbles
      (Google Fonts) для заголовков/лого, Nunito для основного текста.
      Хедер увеличен (крупнее лого, паддинги, шрифт навигации).
      Добавлен дебаг-тумблер `debug: ☀️/🌙` (правый нижний угол) —
      форсирует инверсию цветов вручную, независимо от системной темы,
      временный инструмент, не финальная фича (`src/App.tsx`).
- [x] На широких (4K, ≥1800px) экранах сетка меню — 4 карточки в строке
      вместо 3, контейнер шире (1440px вместо 1100px).

**Важное решение по вёрстке**: вместо точного клонирования CSS
оригинала (Elementor-разметка сама по себе — источник "поехавшей
вёрстки", которую и предстоит чинить) сразу собрана чистая адаптивная
раскладка на CSS grid с нуля, с тем же контентом и структурой разделов.
Это ускоряет проект: не тратим время на копирование того, что всё
равно на этапе 2-3 придётся переделывать.

## Этап 2 — доработка багов

Реальные находки по контенту/картинкам/технике уже собраны в
[`docs/BUGS.md`](BUGS.md) (получено разбором HTML оригинала на этапе 1).
Визуальные баги вёрстки — ниже, типичные кандидаты (уточнится по
факту сравнения с оригиналом):

- Поехавшая вёрстка на разных ширинах экрана (mobile/tablet/desktop).
- Битые/непропорциональные изображения.
- Неконсистентные отступы, шрифты, цвета между секциями.
- Нерабочие или ведущие в никуда ссылки/кнопки.
- Доступность: alt-тексты, контраст, фокус на интерактивных элементах.

Найдено и починено при первом мобильном аудите:

- [x] На мобильной ширине навигация в хедере была просто горизонтальной
      прокручиваемой полоской без вменяемой структуры — заменена на
      гамбургер-меню (кнопка + выпадающий список категорий), телефон в
      хедере скрыт на мобильных (он есть в футере).
      `src/components/Header.tsx`, `src/App.css`.
- [x] Карточки блюд на мобильных шли в 1 колонку и из-за квадратного
      фото на всю ширину экрана растягивались почти на весь экран по
      высоте — сетка переведена на 2 колонки при ширине ≤520px, поменьше
      паддинги/шрифты в карточке. `src/App.css`.
- [x] Карточки и степпер цены выглядели "chunky" и слипшимися на
      мобильных: медиа-блок с компактными размерами (≤520px) в
      `src/App.css` стоял раньше безусловных правил тех же
      селекторов и из-за порядка в каскаде не применялся — рендерились
      десктопные размеры кнопок/паддингов. На 320px активный степпер
      (−/+) вылезал за левый край карточки. Перенесён медиа-блок,
      футер карточки получил `flex-wrap` (степпер переносится на свою
      строку, если не помещается рядом с калоражем), добавлен ещё один
      компактный уровень на ≤360px. Подробности — в
      `docs/CHANGELOG.md`.
- [x] Браузерный UI-аудит хедера (десктоп/планшет/мобильный):
      горизонтальная навигация молча обрезалась на ширинах <1100px без
      намёка на скролл («Напитки» → «На» на 1440px, всего 2 из 9
      пунктов видно на 768px) — точка перехода в бургер-меню поднята
      до `1099px`, навигация уплотнена. Заодно: анимация бургер →
      крестик при открытии, выравнивание хедера с карточками по
      правому краю, центрирование крестика закрытия в корзине, перенос
      номера телефона в Hero CTA на ≤880px чинится столбиком вместо
      равного деления строки. Подробности — в `docs/CHANGELOG.md`.

Найдено, не починено (нужно решение):

- [ ] **`hero.png`/`hero.webp` — это на самом деле логотип-плакат**
      ("RIDER KITCHEN — FEED YOUR TEAM"), не фотография. На хедере с
      `object-fit: cover` при непропорциональных соотношениях сторон
      логотип обрезается и текст хедера налезает на текст в самом
      логотипе. Похоже на временную заглушку — нужно либо настоящее
      фото для хиро-секции, либо другая вёрстка под этот логотип
      (не как фон-фото, а как отдельный элемент).
- [ ] Карточки с длинным описанием визуально "тянут" всю строку сетки
      вверх (grid по умолчанию растягивает соседние карточки в одной
      строке до высоты самой высокой) — разная длина описания даёт
      неровные карточки. Возможное решение — см. идею в этапе 4 (карточка
      с описанием по клику).

Формат: завести чек-лист багов (можно прямо в этом файле или отдельным
`docs/BUGS.md`) — багу, скриншот "было", что поправлено.

## Этап 3 — разбивка на компоненты (сделано)

Цель — не "разбить ради разбивки", а получить структуру, в которой
легко добавлять фичи из этапа 4.

Черновая структура:

```
src/
  components/     # переиспользуемые UI-блоки (Header, Footer, Button, Card...)
  pages/          # секции/страницы сайта (Home, Menu, Contacts...)
  content/        # тексты, меню, цены — отдельно от разметки
  assets/         # изображения, иконки
```

Принципы:

- Компонент — если кусок используется 2+ раза или является явной
  смысловой единицей (карточка блюда, форма заявки, хедер).
  Не выносить в компонент то, что используется один раз и не несёт
  самостоятельного смысла — это лишний слой абстракции.
- Контент (тексты/цены) отдельно от компонентов — проще редактировать
  и презентовать владельцу изменения без переписывания JSX.
- Строгая типизация: описать типы для меню/блюд/цен и т.п. — раз уж
  нравится строгая типизация, это то место, где она даёт больше всего
  пользы (опечатка в поле сразу подсвечивается).

Сделано (включая рефакторинг R0–R6, детали —
[`docs/REFACTOR_PLAN.md`](REFACTOR_PLAN.md)):

- [x] Компоненты выделены: Header, Hero, MenuSection, SetsSection,
      DishCard, Footer, CartDrawer, PriceStepper, CartPdfSheet,
      дебаг-тумблеры темы/шрифтов (итоговая структура — `AGENTS.md`).
- [x] Контент отдельно от разметки: `src/content/` (`menu.ts`,
      `types.ts`, `hero.ts`), строгая типизация меню/цен в
      `src/content/types.ts`.
- [x] Рефакторинг: `App.css` (1234 строки) разбит на 9 партиалов в
      `src/styles/` (01-header … 09-pdf, `App.css` — агрегатор
      `@import`), CSS-бандл совпал байт-в-байт; стейт корзины вынесен
      в хук `src/hooks/useCart.ts`; чистые функции — в `src/lib/`
      (`catalog`, `order`, `format`, `exportPdf`).
- [x] Тестовая инфраструктура: Playwright e2e (`tests/*.spec.ts`,
      4 вьюпорта 320/375/768/1440 — 26 тестов) и юнит-тесты Vitest
      (`tests/unit/` — 67 тестов: `catalog`, `order`, `format`,
      `useCart`, `PriceStepper`); команды `npm run test:unit`,
      `npm run test:e2e`, `npm run test`.

## Этап 4 — новые фичи

Формат: одна фича — один пункт с коротким описанием ожидаемого
поведения, чтобы потом было легко сделать чек-лист для презентации
владельцу.

- [x] **Hero-секция и заголовки**: двухпанельный hero (текст + CTA
      слева, постер-логотип на натуральной пропорции справа — баг с
      обрезкой закрыт), шапки разделов «номер — заголовок — счётчик»,
      `clamp()`-типографика, smooth-scroll с `scroll-padding-top`,
      `prefers-reduced-motion`, `:focus-visible`.
- [x] **Шрифтовая система**: тест вариантов A–E через переключалку
      (роли в CSS-переменных, `data-fonts`, localStorage); выбран
      вариант B (Philosopher + Onest), зашит дефолтом. Светлая тема
      по умолчанию, тёмная — явно через дебаг-тумблер.
- [x] **Новый акцентный цвет**: травяной зелёный `#3a7d44` (тёмная
      тема — мятный `#8ccfa0`) вместо терракотового.
- [x] **Цены-степперы на карточках**: пилюля с инверсией на ховере,
      клик → «− цена +» с `× N` сбоку; цена прижата к нижнему правому
      углу карточки (`margin-top: auto`), заодно смягчает проблему
      «неровных карточек» из этапа 2.
- [x] **Корзина**: стейт `Map<ключ, кол-во>` в App (без
      стейт-менеджера), кнопка в хедере справа (сумма + бейдж),
      drawer справа со степперами, телефон РФ с маской, «Сформировать
      заказ» → WhatsApp с готовым текстом.
- [x] **PDF-выгрузка заказа**: генерация файла и скачивание
      (`jspdf` + `html2canvas-pro` динамическим `import()`);
      раскладка «фото | название+описание | × кол-во | стоимость»,
      позиции без фото — компактным блоком внизу, итог — разбивка по
      разделам + калораж.
- [x] **Калораж**: оценки ккал по составу блюд (в карточках — нижний
      левый угол, в PDF — по позициям и в итоге). ⚠️ Цифры
      демонстрационные: на оригинале КБЖУ нет, подтвердить у владельца
      и заменить реальными.
- [ ] **Карточка блюда с деталями по клику** (идея с мобильного аудита,
      пока не реализовано): на мобильных карточка сейчас растягивается
      по высоте из-за описания под квадратное фото. Предлагается
      сменить раскладку карточки в списке на горизонтальную (фото
      слева, название+цена справа, описание скрыто), а полное описание
      + БЖУ и граммовки открывать по тапу на карточку (модалка/деталка
      блюда). Калорийность уже есть (оценочная), БЖУ/граммовки —
      получить от владельца.
- [ ] **Поднять перцепшн/премиальность через контент, не только вёрстку**:
      сама доставка недорогая, но хочется, чтобы описания блюд и подача
      меню считывались чуть дороже своей реальной цены — не за счёт
      цен, а за счёт формулировок в описаниях и общей подачи меню.
      Важно не переборщить (не превращать в пафосный ресторанный
      текст, который будет не в тон бренду). Требует ревью
      копирайтинга описаний блюд, возможно с владельцем.
- [ ] (заполнить остальное)

## Changelog

Краткая история сделанного ведётся в [`docs/CHANGELOG.md`](CHANGELOG.md).

## Этап 5 — презентация владельцу

- Подготовить сравнение "было / стало" (скриншоты или живой локальный
  сайт рядом с текущим).
- Отдельно выделить: (а) починенные баги, (б) новые фичи — так виднее
  ценность работы.
- Не деплоить никуда без согласия владельца — на этом этапе всё
  остаётся локальным/демо.
